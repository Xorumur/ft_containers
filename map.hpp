#pragma once

#include <utility>


namespace ft {

    template < class Key, class T, class Alloc = allocator<pair<const Key,T> >
    class map {
        public:
            typedef pair<const Key, T> value_type;
        private:

        public:
            map();
    }
}